interface AppRouter {
    root: string;
    pasta: string;
    pizza: string;
    cart: string;
}

export const AppRoutes: AppRouter ={
    root: "/",
    pasta: "/pasta",
    pizza: "/pizza",
    cart: "/cart"
}